const CONSTANTS = {
    DATETIME_FORMAT: 'YYYY/MM/DD hh:mm A',

    // Route
    defaultRoute: '/questions',

    // Regex
    regex: {
        username: /^[\w-\.\d]+$/i,
        email: /[-0-9a-zA-Z.+_]+@[-0-9a-zA-Z.+_]+\.[a-zA-Z]{2,}/i
    }
};

export default CONSTANTS;
